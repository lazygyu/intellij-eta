// Generated by JFlex -- DO NOT EDIT
// This file was generated from src/main/grammars/Eta.flex

package dev.varik.eta.parsing;

import com.intellij.lexer.FlexLexer;
import com.intellij.psi.tree.IElementType;
import static dev.varik.eta.parsing.EtaTokenTypes.*;

public class _EtaLexer implements FlexLexer {

    /** This character denotes the end of file. */
    public static final int YYEOF = -1;

    /** Initial size of the lookahead buffer. */
    private static final int ZZ_BUFFERSIZE = 16384;

    // Lexical states
    public static final int YYINITIAL = 0;
    public static final int IN_ETA_TAG_START = 2;
    public static final int IN_ETA_TAG = 4;

    private static final int ZZ_UNKNOWN_ERROR = 0;
    private static final int ZZ_NO_MATCH = 1;
    private static final int ZZ_PUSHBACK_2BIG = 2;

    private static final String[] ZZ_ERROR_MSG = {
        "Unknown internal scanner error",
        "Error: could not match input",
        "Error: pushback value was too large"
    };

    /** The input device. */
    private java.io.Reader zzReader;

    /** The current state of the DFA. */
    private int zzState;

    /** The current lexical state. */
    private int zzLexicalState = YYINITIAL;

    /**
     * This buffer contains the current text to be matched and is the source of the yytext() string.
     */
    private char[] zzBuffer = new char[ZZ_BUFFERSIZE];

    /** The textposition at the last accepting state. */
    private int zzMarkedPos;

    /** The current text position in the buffer. */
    private int zzCurrentPos;

    /** startRead marks the beginning of the yytext() string in the buffer. */
    private int zzStartRead;

    /**
     * endRead marks the last character in the buffer, that has been read from input.
     */
    private int zzEndRead;

    /** zzAtEOF == true iff the scanner is at the EOF. */
    private boolean zzAtEOF;

    /** Number of newlines encountered up to the start of the matched text. */
    @SuppressWarnings("unused")
    private int yyline;

    /** Number of characters from the last newline up to the start of the matched text. */
    @SuppressWarnings("unused")
    private int yycolumn;

    /** Number of characters up to the start of the matched text. */
    @SuppressWarnings("unused")
    private long yychar;

    /** Whether the scanner is currently at the beginning of a line. */
    @SuppressWarnings("unused")
    private boolean zzAtBOL = true;

    /** Whether the user-EOF-code has already been executed. */
    @SuppressWarnings("unused")
    private boolean zzEOFDone;

    public _EtaLexer(java.io.Reader in) {
        this.zzReader = in;
    }

    public _EtaLexer() {
        this((java.io.Reader) null);
    }

    private boolean zzRefill() throws java.io.IOException {
        if (zzStartRead > 0) {
            zzEndRead += zzFinalHighSurrogate;
            zzFinalHighSurrogate = 0;
            System.arraycopy(zzBuffer, zzStartRead, zzBuffer, 0, zzEndRead - zzStartRead);
            zzEndRead -= zzStartRead;
            zzCurrentPos -= zzStartRead;
            zzMarkedPos -= zzStartRead;
            zzStartRead = 0;
        }

        if (zzCurrentPos >= zzBuffer.length - zzFinalHighSurrogate) {
            char[] newBuffer = new char[zzBuffer.length * 2];
            System.arraycopy(zzBuffer, 0, newBuffer, 0, zzBuffer.length);
            zzBuffer = newBuffer;
        }

        int requested = zzBuffer.length - zzEndRead;
        int numRead = zzReader.read(zzBuffer, zzEndRead, requested);

        if (numRead > 0) {
            zzEndRead += numRead;
            if (Character.isHighSurrogate(zzBuffer[zzEndRead - 1])) {
                if (numRead == requested) {
                    --zzEndRead;
                    zzFinalHighSurrogate = 1;
                }
            }
            return false;
        }

        return true;
    }

    private int zzFinalHighSurrogate = 0;

    @Override
    public final void yybegin(int newState) {
        zzLexicalState = newState;
    }

    public final int yystate() {
        return zzLexicalState;
    }

    public final CharSequence yytext() {
        return new String(zzBuffer, zzStartRead, zzMarkedPos - zzStartRead);
    }

    public final int yylength() {
        return zzMarkedPos - zzStartRead;
    }

    public final char yycharat(int pos) {
        return zzBuffer[zzStartRead + pos];
    }

    public void yypushback(int number) {
        if (number > yylength()) {
            zzScanError(ZZ_PUSHBACK_2BIG);
        }
        zzMarkedPos -= number;
    }

    private void zzScanError(int errorCode) {
        String message;
        try {
            message = ZZ_ERROR_MSG[errorCode];
        } catch (ArrayIndexOutOfBoundsException e) {
            message = ZZ_ERROR_MSG[ZZ_UNKNOWN_ERROR];
        }
        throw new Error(message);
    }

    @Override
    public int getTokenStart() {
        return zzStartRead;
    }

    @Override
    public int getTokenEnd() {
        return zzMarkedPos;
    }

    public int getState() {
        return zzLexicalState;
    }

    @Override
    public void reset(CharSequence buffer, int start, int end, int initialState) {
        char[] chars = buffer.toString().toCharArray();
        this.zzBuffer = chars;
        this.zzCurrentPos = start;
        this.zzMarkedPos = start;
        this.zzStartRead = start;
        this.zzEndRead = end;
        this.zzAtEOF = false;
        this.zzAtBOL = true;
        this.zzLexicalState = initialState;
    }

    @Override
    public IElementType advance() throws java.io.IOException {
        int input;

        while (true) {
            zzMarkedPos = zzCurrentPos;
            zzStartRead = zzCurrentPos;

            if (zzCurrentPos >= zzEndRead) {
                return null; // EOF
            }

            switch (zzLexicalState) {
                case YYINITIAL:
                    return advanceInitial();

                case IN_ETA_TAG_START:
                    return advanceTagStart();

                case IN_ETA_TAG:
                    return advanceInTag();

                default:
                    return null;
            }
        }
    }

    private IElementType advanceInitial() {
        int pos = zzCurrentPos;

        // Check for opening tag: <%_, <%-, <%
        if (pos + 1 < zzEndRead && zzBuffer[pos] == '<' && zzBuffer[pos + 1] == '%') {
            if (pos + 2 < zzEndRead && zzBuffer[pos + 2] == '_') {
                zzCurrentPos = pos + 3;
                zzMarkedPos = zzCurrentPos;
                yybegin(IN_ETA_TAG_START);
                return OPEN_TAG;
            }
            if (pos + 2 < zzEndRead && zzBuffer[pos + 2] == '-') {
                zzCurrentPos = pos + 3;
                zzMarkedPos = zzCurrentPos;
                yybegin(IN_ETA_TAG_START);
                return OPEN_TAG;
            }
            zzCurrentPos = pos + 2;
            zzMarkedPos = zzCurrentPos;
            yybegin(IN_ETA_TAG_START);
            return OPEN_TAG;
        }

        // Consume template data (everything that's not <%)
        int start = pos;
        while (pos < zzEndRead) {
            if (zzBuffer[pos] == '<' && pos + 1 < zzEndRead && zzBuffer[pos + 1] == '%') {
                break;
            }
            pos++;
        }

        if (pos == start) {
            // Single '<' at end of input (not followed by '%')
            pos++;
        }

        zzCurrentPos = pos;
        zzMarkedPos = pos;
        return TEMPLATE_DATA;
    }

    private IElementType advanceTagStart() {
        int pos = zzCurrentPos;

        if (pos < zzEndRead) {
            char c = zzBuffer[pos];
            if (c == '=') {
                zzCurrentPos = pos + 1;
                zzMarkedPos = zzCurrentPos;
                yybegin(IN_ETA_TAG);
                return EQUALS;
            }
            if (c == '~') {
                zzCurrentPos = pos + 1;
                zzMarkedPos = zzCurrentPos;
                yybegin(IN_ETA_TAG);
                return TILDE;
            }
            // No modifier - don't consume, go to IN_ETA_TAG
            yybegin(IN_ETA_TAG);
            // Return next token from IN_ETA_TAG state
            return advanceInTag();
        }

        return null;
    }

    private IElementType advanceInTag() {
        int pos = zzCurrentPos;

        // Check for closing tag: _%>, -%>, %>
        if (pos < zzEndRead) {
            if (pos + 2 < zzEndRead && zzBuffer[pos] == '_' && zzBuffer[pos + 1] == '%' && zzBuffer[pos + 2] == '>') {
                zzCurrentPos = pos + 3;
                zzMarkedPos = zzCurrentPos;
                yybegin(YYINITIAL);
                return CLOSE_TAG;
            }
            if (pos + 2 < zzEndRead && zzBuffer[pos] == '-' && zzBuffer[pos + 1] == '%' && zzBuffer[pos + 2] == '>') {
                zzCurrentPos = pos + 3;
                zzMarkedPos = zzCurrentPos;
                yybegin(YYINITIAL);
                return CLOSE_TAG;
            }
            if (pos + 1 < zzEndRead && zzBuffer[pos] == '%' && zzBuffer[pos + 1] == '>') {
                zzCurrentPos = pos + 2;
                zzMarkedPos = zzCurrentPos;
                yybegin(YYINITIAL);
                return CLOSE_TAG;
            }
        }

        // Consume JS content, skipping %> inside strings and comments
        int start = pos;
        while (pos < zzEndRead) {
            char c = zzBuffer[pos];

            // Single-line comment: skip to end of line
            if (c == '/' && pos + 1 < zzEndRead && zzBuffer[pos + 1] == '/') {
                pos += 2;
                while (pos < zzEndRead && zzBuffer[pos] != '\n') {
                    pos++;
                }
                continue;
            }

            // Multi-line comment: skip to */
            if (c == '/' && pos + 1 < zzEndRead && zzBuffer[pos + 1] == '*') {
                pos += 2;
                while (pos + 1 < zzEndRead) {
                    if (zzBuffer[pos] == '*' && zzBuffer[pos + 1] == '/') {
                        pos += 2;
                        break;
                    }
                    pos++;
                }
                continue;
            }

            // String literals: skip to matching quote
            if (c == '\'' || c == '"' || c == '`') {
                char quote = c;
                pos++;
                while (pos < zzEndRead) {
                    if (zzBuffer[pos] == '\\') {
                        pos += 2; // skip escaped character
                        continue;
                    }
                    if (zzBuffer[pos] == quote) {
                        pos++;
                        break;
                    }
                    pos++;
                }
                continue;
            }

            // Check for closing delimiter
            if (c == '%' && pos + 1 < zzEndRead && zzBuffer[pos + 1] == '>') {
                break;
            }
            if (c == '_' && pos + 2 < zzEndRead && zzBuffer[pos + 1] == '%' && zzBuffer[pos + 2] == '>') {
                break;
            }
            if (c == '-' && pos + 2 < zzEndRead && zzBuffer[pos + 1] == '%' && zzBuffer[pos + 2] == '>') {
                break;
            }
            pos++;
        }

        if (pos == start) {
            // Should not happen, but consume one char to avoid infinite loop
            pos++;
        }

        zzCurrentPos = pos;
        zzMarkedPos = pos;
        return INNER_JS;
    }
}
